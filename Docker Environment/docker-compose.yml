services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "8080:8080"
    volumes:
      - jenkins_home:/var/jenkins_home

  collector:
    build: ./CollectorAPI   # 👈 THIS is the collector
    ports:
      - "8000:8000"
    depends_on:
      - jenkins

volumes:
  jenkins_home:
